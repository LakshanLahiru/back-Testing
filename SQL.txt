create database binance;
use binance;
create table back(openTime varchar(255),closeTime varchar(255),entryPrice double,closePrice double,decision int ,side varchar(255),profit double,ROI double);

select * from back;